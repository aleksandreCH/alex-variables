using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ბიოგრაფია : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int myAge = 13;
        int myName = "alex";
        int mySuraname = "chogovadze";
    }

    // Update is called once per frame
    void Update()
    {

    }
}
